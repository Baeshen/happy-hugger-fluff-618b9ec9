## الهدف

إعادة تشغيل اختبارات RLS الموجودة في `tests/rls/reminder-preference-audit-rls.test.ts` والتحقق من نجاحها.

## الخطوات

1. تشغيل `bunx vitest run tests/rls/reminder-preference-audit-rls.test.ts`.
2. عرض ملخّص النتيجة (نجاح/فشل، عدد الاختبارات، الزمن).
3. إذا فشل أي اختبار: تشخيص السبب وإصلاحه في نفس الجولة.

## لا تغييرات على الملفات

الاختبارات والـ RLS والـ trigger موجودة وسبق التحقق منها. المطلوب تنفيذ فقط.